# Normalize line endings to LF in the repo (avoids CRLF churn on Windows).
* text=auto eol=lf

# Binary assets (none yet, but explicit for the future).
*.png binary
*.jpg binary
*.gif binary
*.ico binary
